/**任务收集器
 * ```
 * const tl = new AggregateList();
 * tl.next = sleep(1)
 * tl.next = sleep(2)
 * await tl.toPromise();
 * ```
 */
export declare class AggregateList<T = any> {
    private _toPromise?;
    private _errors;
    toPromise(): Promise<void>;
    private _total;
    private _finished;
    set next(task: Promise<T> | T);
    private _onSuccess;
    private _onError;
    private _reset;
    private _getError;
    constructor();
}
export declare const wrapAggregateList: <T = any>(cb: (aggregateList: AggregateList<T>) => unknown) => Promise<void>;
