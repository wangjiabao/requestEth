/**任务收集器
 * ```
 * const tl = new TaskList();
 * tl.next = sleep(1)
 * tl.next = sleep(2)
 * await tl.toPromise();
 * ```
 */
export declare class TaskList<T = any> {
    private _task_list;
    toPromise(): Promise<(Awaited<T> | Awaited<T>)[]>;
    set next(task: Promise<T> | T);
    /**
     * @deprecated
     */
    tryToPromise(): (T | Promise<T>)[] | Promise<(Awaited<T> | Awaited<T>)[]>;
}
export declare const wrapTaskList: <T = any>(cb: (taskList: TaskList<T>) => unknown) => Promise<any[]>;
