import { EventEmitter } from "../../event/index.js";
export declare class SequenceHelper extends EventEmitter<{
    doJob: [];
}> {
    constructor();
    private __tasks;
    private __results;
    private __isDoingJob;
    execTask<T = any>(task: Function, ...args: any[]): Promise<T>;
    execTaskWithoutPushResult<T = any>(task: Function, ...args: any[]): Promise<T>;
    private __doJob;
    doAll(): Promise<any[]>;
    /**
     * 清空队列
     *
     */
    clearQueue(): void;
}
