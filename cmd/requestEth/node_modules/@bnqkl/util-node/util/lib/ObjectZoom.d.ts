declare type objValue = string | number | boolean | bigint;
export declare class ObjectZoom<T extends {
    [key: string]: objValue;
}> {
    private _sort?;
    private useindex?;
    constructor(_sort?: string[] | undefined, useindex?: Map<string, string[]> | undefined);
    keysMaps: Map<string, Map<objValue, {
        [key: string]: objValue;
    }>>;
    indexs: Map<string, {
        keyzoom: ObjectZoom<any>;
        valueRef: Map<any, Set<T>>;
    }>;
    fromObject(obj: T): T;
}
export {};
