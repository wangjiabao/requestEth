/**队列策略 */
export declare enum THROTTLE_WRAP_PLOT {
    /**等待任务执行完成才开始下一个任务 */
    WAIT_RESULT_RETURN = 0,
    /**不等待任务执行完成，只要一开始执行，就能开始执行下一个任务 */
    NO_WAIT_EXEC_TIME = 1
}
/**
 *
 * @param ms
 */
export declare function throttleWrap(ms?: number, plot?: THROTTLE_WRAP_PLOT): (target: any, name: any, descriptor: PropertyDescriptor) => PropertyDescriptor;
