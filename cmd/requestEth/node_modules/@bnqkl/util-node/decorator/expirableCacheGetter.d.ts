/**
 * 带过期功能的缓存器
 * - 时间模式下
 *   - 如果是频繁获取的行为， 可以使用timer
 *   > 会创建setTimeout定时器，会多消耗内存来缓存定时器函数
 *   - 如果是低频率获取的行为，可以使用checker
 *   > 单纯使用缓存的时间来判定，会多消耗判定的CPU
 *
 * @param expirationCondition
 */
export declare function expirableCacheGetter(expirationCondition: {
    timeout: number;
    mode?: "timer" | "checker";
} | (() => boolean)): (target: any, key: string | symbol, descriptor: PropertyDescriptor) => PropertyDescriptor;
