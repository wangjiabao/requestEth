export declare class PropArrayHelper<T = any> {
    pid: string;
    constructor(pid?: string);
    PA_ID_KEY: symbol;
    CLASS_PROTO_ARRAYDATA_POOL: Map<string | Symbol, Map<string, T[]>>;
    get(target: any, key: string | symbol): Set<T>;
    PA_ID_VALUE: number;
    add(target: any, key: string | symbol, value: T): void;
    remove(target: any, key: string | symbol, value: T): void;
}
