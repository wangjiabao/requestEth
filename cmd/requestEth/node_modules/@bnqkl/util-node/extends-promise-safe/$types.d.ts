export declare type $SafeThendCache<T> = {
    cbs?: {
        resolves: Set<(value: T) => unknown>;
        rejects: Set<(reason: any) => unknown>;
    };
    promise: PromiseLike<unknown>;
};
export declare type $PromiseRaceOptions = {
    throwWhenNoRacer: boolean;
};
