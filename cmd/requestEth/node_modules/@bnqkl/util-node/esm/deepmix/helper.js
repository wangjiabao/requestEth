/**对象是否可以混合，除了object，其它都不行（array，function，Map，Set，ArrayBufferLike等待） */
export function mergeAble(value) {
    return (typeof value === "object" &&
        value !== null &&
        !(Symbol.iterator in value));
}
