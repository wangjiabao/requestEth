import { $ReactiveStream } from "./$types.js";
export declare class ReactiveStream<T> {
    private _src;
    constructor(_src: AsyncIterableIterator<T>);
    filter<R extends T = T>(filter: $ReactiveStream.Filter<T, R>): AsyncGenerator<Awaited<R>, void, unknown>;
    map<R>(map: $ReactiveStream.Map<T, R>): AsyncGenerator<Awaited<R>, void, unknown>;
    tee(): ReactiveStream<T>;
}
declare const continue_symbol: unique symbol;
declare const break_symbol: unique symbol;
export declare const FOR_CONTROLLER: Readonly<{
    readonly continue: typeof continue_symbol;
    readonly break: typeof break_symbol;
}>;
export {};
