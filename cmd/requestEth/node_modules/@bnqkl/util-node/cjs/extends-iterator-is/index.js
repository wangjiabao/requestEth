"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isGeneratorInstance = exports.isAsyncGeneratorInstance = void 0;
const isAsyncGeneratorInstance = (maybe) => {
    if (maybe && maybe.constructor) {
        return (typeof maybe.next === "function" &&
            typeof maybe.return === "function" &&
            typeof maybe.throw === "function" &&
            typeof maybe[Symbol.asyncIterator] === "function" &&
            maybe[Symbol.asyncIterator]() === maybe);
    }
    return false;
};
exports.isAsyncGeneratorInstance = isAsyncGeneratorInstance;
const isGeneratorInstance = (maybe) => {
    if (maybe) {
        return (typeof maybe.next === "function" &&
            typeof maybe.return === "function" &&
            typeof maybe.throw === "function" &&
            typeof maybe[Symbol.iterator] === "function" &&
            maybe[Symbol.iterator]() === maybe);
    }
    return false;
};
exports.isGeneratorInstance = isGeneratorInstance;
