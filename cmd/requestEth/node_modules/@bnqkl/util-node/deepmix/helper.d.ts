/**对象是否可以混合，除了object，其它都不行（array，function，Map，Set，ArrayBufferLike等待） */
export declare function mergeAble<V>(value: V): MergeAble<V>;
export declare type MergeAble<V> = V extends object ? V extends {
    [Symbol.iterator]: unknown;
} ? never : V : never;
