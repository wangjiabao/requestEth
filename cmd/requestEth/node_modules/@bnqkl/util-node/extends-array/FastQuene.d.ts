/**快速队列
 * 提供更简单更快的push
 * 以及更快的shift：保证不会生成多余的array副本
 */
export declare class FastQuene<T> {
    private _size;
    private _arr;
    private _firstCursor;
    push(item: T): T;
    shift(): T | undefined;
    remove(item: T): boolean;
    private _updateSize;
    get size(): number;
}
