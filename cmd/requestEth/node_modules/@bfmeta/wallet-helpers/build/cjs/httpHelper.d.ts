/// <reference types="node" />
/// <reference types="node" />
import * as http from "http";
import * as https from "https";
export declare class HttpHelper {
    constructor();
    checkHttp(url: string): typeof http | typeof https;
    sendPostRequest<T>(url: string, argv: {
        [key: string]: any;
    }): Promise<T>;
    sendGetRequest<T>(url: string, argv?: {
        [key: string]: any;
    }): Promise<T>;
    sendApiGetRequest<T>(url: string, argv?: {
        [key: string]: any;
    }, headers?: {
        [key: string]: any;
    }): Promise<T>;
}
