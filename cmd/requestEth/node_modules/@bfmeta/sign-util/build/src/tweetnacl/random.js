"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports._randomBytes = exports.randomBytes = void 0;
const array_1 = require("./array");
function randomBytes(n) {
    const b = new array_1.ByteArray(n);
    _randomBytes(b, n);
    return b;
}
exports.randomBytes = randomBytes;
const QUOTE = 1 << 16;
function _randomBytes(x, n) {
    for (let i = 0; i < n; i += QUOTE) {
        crypto.getRandomValues(x.subarray(i, i + Math.min(n - i, QUOTE)));
    }
}
exports._randomBytes = _randomBytes;
//# sourceMappingURL=random.js.map