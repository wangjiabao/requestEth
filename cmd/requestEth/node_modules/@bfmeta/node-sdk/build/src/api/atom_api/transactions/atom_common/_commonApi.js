"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CommonApi = void 0;
class CommonApi {
    networkHelper;
    constructor(networkHelper) {
        this.networkHelper = networkHelper;
    }
    async sendPostRequest(argv) {
        const apiPath = `${this.networkHelper.TRANSACTION_SERVER_URL_PREFIX}${this.EXEC_API_PATH}`;
        try {
            const result = await this.networkHelper.createTransaction(apiPath, argv);
            return result;
        }
        catch (e) {
            const errorInfo = {
                success: false,
                error: {
                    code: "7001",
                    message: `request api ${apiPath} error`,
                    description: e.message,
                },
            };
            return errorInfo;
        }
    }
}
exports.CommonApi = CommonApi;
